# Blog cover images

Put blog cover images in this directory. WebP, JPG, and PNG are recommended.

Use sensible image dimensions and keep file sizes small enough for fast page loads. Store the public path in the database, for example: `/images/blog/my-post.webp`.
